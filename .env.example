# NoVoice Desktop — Build-time environment variables
# Copy to .env.production before running: npm run electron:build

# The server URL baked into the built app.
# Users can override this on the login screen at any time.
VITE_SERVER_URL=46.224.71.180
